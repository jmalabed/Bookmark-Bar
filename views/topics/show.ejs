<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <link type='text/css' href="/style.css" rel="stylesheet"/>
    <title></title>
  </head>
  <body>
    <h1>
      <!-- <img src="<%=topic%>" alt="" style ='width:200px; height:200px'> -->
       <%=topic.name%>
    </h1>
    <div class='col'>
      <ul> <% resources.forEach(resource => { %>
        <% if (topic.name === resource.topicId) { %>
          <li>
             <a href='<%=resource.url%>'> <%= resource.name %></a>

            <form action="/topics/<%=topic._id%>/<%=resource._id%>/like?_method=PUT" method="post">

              Likes: <%=resource.likes%>
              <input type="hidden" name="likes" value="<%=resource.likes%>">
              <input type="submit" value="LIKE">
            </form>

            <p> <%= resource.description %> </p>
            <a href='/topics/<%=resource._id%>/edit'>edit</a>

          </li>
        <% } %>
      <% }) %>
      </ul>
    </div>
    <a href="/topics/<%=topic._id%>/new">Add Resource!</a>
    <a href='/topics'>home</a>
  </body>
</html>
